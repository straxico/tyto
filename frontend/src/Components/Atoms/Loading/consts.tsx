const TYPE_OPTIONS = {
  BUTTON_LOADER: 'buttonLoader' as LoadingType,
  SEARCH_LOADER: 'searchLoader' as LoadingType,
  BOX_LOADER: 'boxLoader' as LoadingType,
  PAGE_LOADER: 'pageLoader' as LoadingType,
  INLINE_LOADER: 'inlineLoader' as LoadingType,
};

export default TYPE_OPTIONS;
